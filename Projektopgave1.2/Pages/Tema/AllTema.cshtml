@page
@model Projektopgave1._2.AllTemaModel
@{
    ViewData["Title"] = "AllTema";
}
<div class="overskrift"><h1>Temaer</h1></div>
<table class="overskrift table">
    <thead>
        <tr>
            <th>Name </th>
            <th>Id</th>
            <th>Description</th>
            <th>Image</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Temaer.Count != 0)
        {
            @foreach (var item in Model.Temaer)
            {
                <tr class="overskrift">
                    <td>
                        @item.Name
                    </td>
                    <td>
                        @item.Id
                    </td>
                    <td>
                        @item.Description
                    </td>
                    <td>
                        <img src="~/images/@item.ImageName" width="140" height="70">
                    </td>
                    <td>
                        <p>
                            <a asp-page="EditTema" asp-route-id="@item.Id">Rediger</a> |
                            <a asp-page="DeleteTema" asp-route-id="@item.Id">Slet</a> |
                            <a asp-page="SeTema" asp-route-id="@item.Id"> Se Tema</a>  |

                        </p>
                    </td>
                </tr>
            }}
    </tbody>
</table>
<a asp-page="AddTema"
   class="overskrift text-dark btn btn-success col-auto pull-left">
    Opret tema
</a>

<a asp-page="../Kunstner/AllKunstner"
   class="overskrift text-dark btn btn-warning col-auto pull-right">
    Se kunstnere
</a>
